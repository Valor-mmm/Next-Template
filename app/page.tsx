export default function Default() {
  return (
    <main>
      This site should never be shown. It is only here because next requires it.
      {/* TODO create an alerting to get notified if this site is shown to a user */}
    </main>
  );
}
